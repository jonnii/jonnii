[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# For persistent SSH host keys, add a volume mount in Railway dashboard:
# Mount path: /app/.ssh
# This prevents "HOST KEY CHANGED" warnings on redeployment
